# For local dev, leave as-is. When deployed to Vercel, set this env var in
# the Vercel project settings to your Render backend URL, e.g.
# VITE_SERVER_URL=https://listen-together-server.onrender.com
VITE_SERVER_URL=http://localhost:4000
